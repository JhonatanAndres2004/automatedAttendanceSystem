name: CI Push On Main

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted 
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Execute initial command
        run: |
          echo "¡Push on main!"
